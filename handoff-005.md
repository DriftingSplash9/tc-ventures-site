# handoff-005 — tc-ventures.ca

**Written:** 2026-09-16
**Covers:** 2026-09-17 → 09-16 (handoff-004 was dated a day ahead; this one uses
the real date). Thomas answered the two pending graph questions — **projects page,
3D** — and the demo was built, wired and tested against that answer.
**Status at wrap:** the demo is **written and verified locally**, in a headless
Chromium against the real files. It has **not** been deployed and has not been
seen serving on tc-ventures.ca.

---

## 0. Read this first if you are a fresh agent

1. This file, top to bottom. §2 "Traps" before you touch git.
2. `claude/thomas-study.md` in the Claude project "TC 'Ventures" — the voice study.
   Read it before writing any copy in Thomas's voice.
3. `reviews/copy-review-001.md` — his rulings on every block of site copy.
4. `briefs/brief-001-wow-and-contact.md` — the wow brief. Contact is done; the
   graph demo is now built, and the brief's constraints are what it was built to.
5. `README.md`.

Then say what you understand the next job to be, and check before building.
§5 defines how you write the handoff that replaces this one. Follow it exactly.

---

## 1. What this is

**tc-ventures.ca** — Thomas Cheesman's hiring-facing portfolio. Hand-written static
HTML, no framework, no build step, no CMS. Cloudflare Workers static assets,
deploy-on-push from GitHub. Deliberately separate from thomascheesman.ca.

| | |
|---|---|
| Repo | `DriftingSplash9/tc-ventures-site` — **public** since 2026-09-16 |
| Local | `C:\Users\thoma\Desktop\My Files\Website Projects\tc-ventures site` |
| Host | Cloudflare Worker `tc-ventures-site` (static assets only) |
| Config | `wrangler.jsonc` → serves `./public`, `not_found_handling: 404-page` |
| Deploy | push to `main` → Cloudflare builds → `npx wrangler deploy` |
| Also live at | `tc-ventures-site.thomasmcheesman.workers.dev` (INFRA-1) |
| Registrar | WordPress.com (registration only); DNS on Cloudflare |
| Contact email | `thomas@tc-ventures.ca` — live, forwards |
| Fonts | self-hosted in `public/assets/fonts/` |
| Résumé | `resume/resume-source.html` → headless Chromium → `public/assets/Thomas-Cheesman-Resume.pdf` |
| Pages | `index`, `projects`, `background`, `contact`, `404` |
| **Graph demo** | `public/assets/graph-demo.js` + `gp-budget-graph.json` + prebuilt `3d-force-graph.min.js`; data regenerated by `scripts/export-gp-budget.py` |

## 2. What was done (2026-09-16)

**G-5 answered by Thomas: projects page, 3D.** Both decisions closed; the demo was
built to them.

**The demo.** Lives inside the Economic Report Influence Graph article on
`projects.html`, immediately after the "no document, no edge" paragraph — so the
A/B/C grading is explained before a reader can click an edge and see one. It is:

- a still (`assets/img/graph-gp-budget.webp`, 1280×800, ~40KB) with a
  **"Load the live graph"** button over it;
- on click, a fetch of the data and the renderer, then a live 3D scene in place of
  the still — colour by tier, size by whole-corpus in-degree, arrows pointing at
  what a report depends on;
- click a line → the panel underneath shows the grade, the relationship in words,
  the quoted basis, and a link to the document it was read out of. Click a sphere →
  title, publisher, tier, and how much of the corpus rests on it.

**The data.** `gp-budget` closure, exported fresh: **54 reports, 76 dependencies,
22 A / 48 B / 6 C** — identical to the numbers scouted in handoff-004, so the corpus
has not moved under this slice. 58KB of JSON with the `basis` text carried in full.

**`scripts/export-gp-budget.py`** regenerates that file from a clone of
Reports-Clustering. It reads the research slices as JSON and the two seed `.ts`
files with `json5`, which is the same join `src/data/index.ts` does but without a
Node toolchain. Re-run it whenever the corpus moves; its header says how.

**Basis text is cleaned at export, never rewritten.** Two edges opened with a
`RESEARCHED <date> (<round>).` log prefix, and one trailed off into how the PDF was
fetched — naming Thomas, `curl`, `pdftotext` and a `notes/` path. The exporter
strips the prefix, drops trailing sentences about *retrieval* rather than about what
the document *says*, and caps the rest at 520 characters with the source link left
underneath. No claim is altered and no grade is touched.

**Verified, headless Chromium, against the real files:**

| Check | Result |
|---|---|
| JavaScript off | Button absent; heading, prose, still, legend and the five-step written chain all present |
| Before the click | No request for `3d-force-graph.min.js` or the JSON. Nothing 3D downloads on page load |
| After the click | Both fetched, canvas renders, camera frames the graph |
| Edge click | Panel fills with grade + quoted basis + source link |
| Node click | Panel fills with title, publisher, tier, authority |
| 390px, dark | Renders; `scrollWidth` 390 — no horizontal overflow |
| 1280px, light | Renders |
| `prefers-reduced-motion` | Layout solved before the first frame; two canvas reads 2s apart are byte-identical, so it is genuinely still, not slowed |

**Two bugs found and fixed in that pass**, both worth knowing:

- `[hidden]` was doing nothing. `.btn`, `.gd__stage` and `.gd__still` all set
  `display`, which outranks the user agent's `[hidden] { display: none }`. The
  button was showing with JavaScript off and an empty canvas box was showing before
  the click — the two things the block exists to avoid. Fixed with
  `.gd [hidden] { display: none !important; }`.
- `zoomToFit` fits a bounding **sphere**. On a graph this elongated that left it
  swimming in a third of the frame. Replaced with a bounding-**box** fit that
  computes the camera distance from the actual extents and the viewport aspect.
  Both the poster render and the live scene use it.

### Traps worth knowing

- **Git in a Cowork-mounted folder cannot unlink files.** `git status` creates
  `.git/index.lock` and can't remove it; commits leave `HEAD.lock`, `ORIG_HEAD.lock`,
  `objects/maintenance.lock` and `objects/*/tmp_obj_*` behind. GitHub Desktop then
  refuses with "a lock file already exists". Rule: run read-only git with
  `GIT_OPTIONAL_LOCKS=0`, and after any write `mv` every non-stale `*.lock` aside.
  Better: don't run git in the mount at all; compare against the live site with curl.
- **GitHub Desktop merges, it does not force-push.** A rewritten local branch gets
  pull-merged, and a merge takes the other side's deletions. This is how the Bare Your
  Rare theme lost 17 files on the live server for a day (2026-09-16/17; fixed with a
  forward commit). Any history rewrite must be pushed from the command line.
- **Running the graph repo's own npm scripts was refused** in this session (executing
  code from an external repo). That is why the exporter is a standalone Python file
  rather than `tsx scripts/...`. It produces byte-identical output to its own
  previous run, and the closure it computes matched handoff-004's scouted numbers
  exactly, which is the check that it joins the corpus the same way.
- **Committing a file to the device re-encodes images.** The poster arrived as a
  valid 2560×1600 WebP but 41,628 bytes rather than the 35,860 that left. Verify a
  committed image by staging it back, not by trusting the byte count.
- **The Facebook rule.** His public Facebook is linked from thomascheesman.ca. He has
  ruled it "between you and me only" — never material for this site.
- The Cowork default folder `Desktop\My Files\TC Ventures` is still empty and still
  not this repo. This session worked in the real repo via a granted folder.

## 3. Current design

Light paper, near-black ink, one deep-teal accent (`#0F5F6B`), dark mode via
`prefers-color-scheme`. Familjen Grotesk / Source Serif 4 / IBM Plex Mono. Confirmed;
stop re-litigating it.

**Standing rules:**
- `object-fit: contain`, never `cover`.
- Content must render without JavaScript. JS allowed; dependencies and a build step
  are not — prebuilt bundles copied into `assets/` only.
- Fonts self-hosted. No third-party font request.
- No phone number on the site. It is in the résumé PDF.
- **Never publish an exact node, edge, report or grade count.** Round or describe.
  The demo copy says "fifty-odd published reports and seventy-odd dependencies"; the
  real 54/76 lives in this file and in the exporter's header, not on the page.
- **No vanity metrics.** No line counts.
- **Do not invent dates or figures.** Unknown → ask Thomas.
- Hajdu-Cheney syndrome is named on purpose. Symptom detail is not.
- WordPress stays once per spec table as a hiring keyword; out of headline prose.
- Nothing familial, nothing from Facebook.
- The GPRS logo is not this site's favicon.
- Edit `resume/resume-source.html` and re-render. Never edit the PDF. Measure page
  count after any content change; the target is exactly two pages, and page 2 is full.
- Melanie and Thomas are "the parents" of the three children, never "co-parents";
  the relationship is not otherwise characterized anywhere.

**New rules, from the demo:**
- **The 3D canvas keeps its own dark ground in both themes** — the same `#060913`
  the still screenshots sit on. A scene lit for paper is unreadable, and flipping it
  per theme means two sets of node colours to keep honest. The panel under it stays
  on paper, because it is text.
- **Nothing heavy loads before a click.** The renderer is 1.3MB. If anything is ever
  added that makes it load on page view, the block has lost its point.
- **`basis` is quoted, never paraphrased.** The exporter may drop retrieval chatter
  and truncate; it may not rewrite a claim or soften a grade.
- **The written chain under the demo is the accessible equivalent**, not decoration.
  A canvas has no keyboard path into it. If the chain is ever cut, the block stops
  meeting the accessibility claim the site makes elsewhere.

## 4. Open items — carry these forward until closed

### COPY
| # | Item | Notes |
|---|---|---|
| C-2 | Sentences unread by Thomas | Projects page and 404 copy remain unread by him — and that now includes the whole demo block: the "traced all the way back" lede, the "one path through it, in words" paragraph, the panel hint and the legend line. |

### GRAPH
| # | Item | Notes |
|---|---|---|
| G-4 | Rocket Lander has no source link | He can make the repo public. |
| G-6 | Poster is a headless render, not a hand-made image | Regenerating it means re-running the export, loading the same bundle and data in headless Chromium, warm-starting the layout, fitting the bounding box, screenshotting and cropping to the content box at 16:10. Worth scripting into `scripts/` if the corpus moves often. |
| G-7 | Roughly a quarter of link midpoints are unclickable | They sit behind nodes or other lines. `linkHoverPrecision(5)` already widens the hit test; raising it further starts stealing clicks from nodes. Acceptable as is — a reader clicks a visible segment, not a midpoint. |
| G-8 | No keyboard path into the 3D scene | Inherent to a canvas. The written chain carries the same evidence for a keyboard or screen-reader user. A future pass could add a focusable list of all edges with the same panel behaviour. |

### INFRA
| # | Item | Notes |
|---|---|---|
| INFRA-1 | workers.dev duplicate | `"workers_dev": false`, `"preview_urls": false` once settled. |
| INFRA-2 | Favicon is a placeholder | Thomas is finding the TC mark. |
| INFRA-4 | Permanent email undecided | `thomas@tc-ventures.ca` works; he wants a non-general address within weeks. |
| INFRA-5 | Two scratch files left in `_unused/` | `gd-block.html` and `graph-demo.css` — the staging copies of the block markup and the demo stylesheet, both already merged into `projects.html` and `style.css`. Deletion is blocked in a Cowork mount; Thomas can delete them. `_unused/` is not served. |

### DOMAIN
| # | Item | Notes |
|---|---|---|
| D-1 | WordPress.com still claims the domain | Harmless; detach when convenient. |
| D-2 | WP.com plan auto-renew | Do not cancel anything without confirming DNS for the three live sites is unaffected. |
| D-3 | Professional Email expiry ~2026-10-08 | Related to INFRA-4. |

### OTHER REPOS
| # | Item | Notes |
|---|---|---|
| O-1 | Deployed graph bundle on thomascheesman.ca is stale (~302 reports) | Rebuild command in that repo's `functions.php` comment. |
| O-2 | `/projects` on thomascheesman.ca has third-person leakage | Reads as an unedited draft. Not this repo. |
| O-3 | thomascheesman.ca "long way round" says Keg 2001 | Thomas confirmed 2002. One-word fix over there. |
| O-4 | **bareyourrare history still contains `permits/` (his address) and `3.jpg`** | Commits 24a7e72 and 2014bed on GitHub. Live server is clean; forward-fix pushed 09-17. Thomas chose to do the rewrite + command-line force-push + GitHub support purge later. Quarantined copies: `My Files\_Quarantine\from-bareyr-repo(-2)`. |
| O-5 | `bareyr\.git` has `*.stale` lock files and `tmp_obj_*` junk | Cosmetic; Thomas to delete. |

**Closed this session:** G-5 (Thomas answered: projects page, 3D); G-1 (wow shipped —
still plus load-on-click, no auto-load); G-2 (demo data exported and in the repo).
Nothing was killed.

## 5. HOW TO WRITE THE NEXT HANDOFF

**This section is fixed. Copy it forward verbatim into every handoff.**

### Naming and location

- Handoffs live at repo root: `handoff-001.md`, `handoff-002.md`, `handoff-003.md`…
- **Zero-padded to three digits.** Sequential, never reused, never renumbered.
- One handoff per meaningful session. A session that changed nothing does not earn one.
- **The highest-numbered file is the only current one.** Everything below it is
  history. Do not read old handoffs unless you are chasing why a decision was made.

### Forking

Work will fork — design, contact page, the graph embed, the Godot build, copy.
**Do not fork the file series.** Numbering stays one straight line. Instead:

- Every open item carries a **workstream tag** in SCREAMING-CASE: `DESIGN`,
  `CONTACT`, `GRAPH`, `LANDER`, `COPY`, `INFRA`, `DOMAIN`.
- §4 (open items) is grouped by tag once there is more than one tag in play.
- If two agents work in parallel, each writes its own numbered handoff and the
  later one merges the earlier one's open items into its §4. Nothing is dropped
  silently — an item leaves §4 only when it is **done** or **explicitly killed by
  Thomas**, and the handoff says which.

### Required sections

Every handoff has these, in this order, with these numbers:

```
0. Read this first if you are a fresh agent
1. What this is            — stack table. Update only when the stack changes.
2. What was done           — THIS SESSION only. Not a changelog of all time.
3. Current design          — the rules a new agent would otherwise break.
4. Open items              — the table. Tagged. Carried forward.
5. HOW TO WRITE THE NEXT HANDOFF   — verbatim copy of this section.
6. Next up                 — what the next agent should actually do, in order.
```

### Rules

- **State, not narrative.** "The X is broken because Y" beats a story about
  discovering it. Nobody needs your journey.
- **Traps are worth more than successes.** If something cost you an hour, write it
  down in one sentence so it costs the next agent nothing. The stale-clone and
  `index.lock` notes in handoff-001 §2 are the model.
- **Never claim something is done that you have not verified.** "Deployed" means you
  saw it serve. "Written" means it is on disk. Say which.
- **Do not state git state and do not tell Thomas to commit.** That is his routine.
- Keep it under ~400 lines. If §2 is getting long, you are writing a diary.

## 6. Next up

In order:

1. **See the demo serve.** Once it is live, open `/projects.html` on the real domain
   and confirm: nothing heavy on load, the button appears, the scene renders, an edge
   click fills the panel. Only then is it "deployed".
2. **Get Thomas's ruling on the demo copy** (C-2) — the lede, the "one path through
   it" paragraph, the panel hint, the legend line.
3. **Favicon** when Thomas supplies the mark (INFRA-2). One file swap.
4. **Close INFRA-1** once the demo is live and settled.
5. **Later:** the Godot rocket lander web build. Tag: `LANDER`.
