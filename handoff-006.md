# handoff-006 — tc-ventures.ca

**Written:** 2026-09-16
**Covers:** the same day as handoff-005, after Thomas returned it annotated. His
rulings closed six items and opened four. This handoff is the state after acting
on every one of them.
**Status at wrap:** everything below is **written and verified locally**, in a
headless Chromium against the real files. Nothing has been deployed. The graph
bundle on thomascheesman.ca is **written into the theme**, not yet uploaded.

---

## 0. Read this first if you are a fresh agent

1. This file, top to bottom. §2 "Traps" before you touch git or a rebuild.
2. `claude/thomas-study.md` in the Claude project "TC 'Ventures" — the voice study.
   Read it before writing any copy in Thomas's voice.
3. `reviews/copy-review-001.md` — his rulings on every block of site copy.
4. `briefs/brief-001-wow-and-contact.md` — the wow brief.
5. `README.md`.

Then say what you understand the next job to be, and check before building.
§5 defines how you write the handoff that replaces this one. Follow it exactly.

---

## 1. What this is

**tc-ventures.ca** — Thomas Cheesman's hiring-facing portfolio. Hand-written static
HTML, no framework, no build step, no CMS. Cloudflare Workers static assets,
deploy-on-push from GitHub. Deliberately separate from thomascheesman.ca.

| | |
|---|---|
| Repo | `DriftingSplash9/tc-ventures-site` — **public** since 2026-09-16 |
| Local | `C:\Users\thoma\Desktop\My Files\Website Projects\tc-ventures site` |
| Host | Cloudflare Worker `tc-ventures-site` (static assets only) |
| Config | `wrangler.jsonc` → serves `./public`, `not_found_handling: 404-page`, **`workers_dev: false`, `preview_urls: false`** |
| Deploy | push to `main` → Cloudflare builds → `npx wrangler deploy` |
| Registrar | WordPress.com (registration only); DNS on Cloudflare |
| Contact email | `thomas@tc-ventures.ca` — live, forwards |
| Fonts | self-hosted in `public/assets/fonts/` |
| Résumé | `resume/resume-source.html` → headless Chromium → `public/assets/Thomas-Cheesman-Resume.pdf` |
| Pages | `index`, `projects`, `background`, `contact`, `404` |
| Graph demo | `public/assets/graph-demo.js` + `gp-budget-graph.json` + prebuilt `3d-force-graph.min.js`; data regenerated by `scripts/export-gp-budget.py` |
| Scratch | `_to_delete/` at repo root — Cowork cannot delete files, so anything disposable is moved there for Thomas |

**Other repos this session touched.** `tc-ventures-child-theme` (thomascheesman.ca)
and `Reports Clustering` (the graph). Both are under `Desktop\My Files\`.

## 2. What was done (2026-09-16, after the annotated handoff-005)

**Four sites, not three** (his ruling on C-2). `index.html`, `projects.html` and
`contact.html` now say four and count this site among them. The Projects section
closes with: *"The fourth is this one, which is the only one of the four with no
WordPress under it — hand-written, no framework, no build step."* The `index.html`
spec row reads "…custom WordPress themes; this one hand-written, no framework".

**INFRA-1 closed in config.** `workers_dev: false` and `preview_urls: false` are in
`wrangler.jsonc` with a comment saying why. **It takes effect on the next deploy**,
so the workers.dev URL is still live until then.

**G-7, the tangle.** Built the hover-and-focus behaviour rather than a magnifier — a
real lens means rendering the scene twice and it fights the orbit controls.
Hovering a report or a line dims everything unconnected; moving off restores it
instantly. Clicking a report flies the camera to it and fills the panel; clicking
the background resets both. **Nothing moves a node.** The positions are the
layout's answer, and spreading neighbours apart to make a click look tidy would be
drawing something the data did not say. Dimming is done in the colour, because
`nodeOpacity` is one global on the whole scene and has no per-node form.

**G-6, the poster.** His screenshot is of the **whole application**, not the
gp-budget slice, so it could not be the demo's still — the still has to show what
the button loads. It is a better image than what was there, though, so with his
ruling it replaced `graph-everything.webp` as the opening figure on Projects, at
`assets/img/graph-app-ui.webp` (1900×886, browser chrome and taskbar cropped off).
The caption and alt text were rewritten to describe what is actually in it, and
`og:image` on `index`, `background` and `contact` was repointed. The demo keeps its
own headless render of the gp-budget slice.

**O-1, the stale graph on thomascheesman.ca — rebuilt and written into the theme.**
`npm run gen` then `npx vite build --base=… --outDir=dist-wp-2` in Reports
Clustering; output copied into `tc-ventures-child-theme/assets/report-graph/`. It
carries the current corpus, and the `functions.php` comment now records the three
ways that rebuild goes wrong (below).

**O-3, the Keg dates.** 2001 → 2002 and twelve/12 → eleven/11 in `page-thomas.php`
(the long way round), `page-hcs.php`, and `inc/heritage/cheesmans-story-body.php`.
The other 2001s — the legal name change at twenty — are correct and were left.

**O-4, the address in bareyourrare history.** Command-by-command instructions are
written to `My Files\_Quarantine\bareyourrare-history-purge.md`: private → mirror
clone outside the mount → `git-filter-repo --invert-paths` → verify → force-push
all refs from the command line → GitHub Support cache purge → public again. **Not
run.** It rewrites published history and that is his to trigger.

**G-4, the Rocket Lander — page built, waiting on the export.** It *is* a git repo
(`Rockets\rocket-lander\.git`) but has **no remote**, so nothing is on GitHub. Godot
**4.7**, renderer **GL Compatibility** — the right renderer for web, so no engine
change is needed. There was **no `export_presets.cfg`**: a Web export had never been
configured. Thomas is installing the Web export template now.

`public/lander.html` and `public/assets/lander.js` are written and tested against a
stub build. The page is linked from the Rocket Lander section of Projects and is
**not in the primary nav** — a work in progress does not earn a nav slot.

Two decisions Thomas made: **the 38-knob panel ships**, and the project goes on the
site **as a work in progress**. His words: *"I cannot get it to land, it is a work in
progress and not ready for a resume."* The page says exactly that, in the lede: *"It
flies. I cannot land it yet, and neither will you."* That framing is the point — the
site's whole argument is that claims carry their grade, and a page that admits an
unsolved landing is worth more than one that implies a finished game.

**How the embed works.** An **iframe** at `/lander/index.html`, not the engine wired
into the page by hand. The Godot export ships an index.html, loader, `.wasm` and
`.pck` that expect to own the document; dropping the exported folder in untouched
means an engine upgrade is a folder swap rather than a re-integration. Same
load-on-click contract as the graph demo — verified: **nothing under `/lander/` is
requested until the button is pressed**. Sandboxed to
`allow-scripts allow-same-origin allow-pointer-lock`; the page focuses the frame on
load, because the click that loaded it landed on the button, not the game.

**Tab was reassigned to the backtick key.** Tab moves focus in a browser and is the
only way a keyboard user leaves a canvas, so binding the tuning panel to it either
traps the reader or never opens. The page documents <kbd>`</kbd>. **The build must
match** — that key change has to happen in `tuning.gd` before the export is final.
`Space` and `Escape` also behave differently in a browser (page scroll; exits
pointer lock) and are worth a look while he is in there.

### Traps worth knowing

- **Git in a Cowork-mounted folder cannot unlink files.** `git status` creates
  `.git/index.lock` and can't remove it. Rule: read-only git with
  `GIT_OPTIONAL_LOCKS=0`; better, don't run git in the mount at all.
- **GitHub Desktop merges, it does not force-push.** A merge takes the other side's
  deletions — how the BYR theme lost 17 files on 2026-09-16. Any history rewrite
  goes through the command line.
- **Cowork cannot delete anything in a mount.** `rm` fails, and `mv` of a
  *directory* fails too, because that is a delete of the source. Files move fine.
  Hence `_to_delete/` in each repo, and hence `--outDir=dist-wp-2`: vite empties its
  output directory before writing, which fails on an existing one.
- **The graph rebuild has three traps, now recorded in `functions.php`:**
  1. `npm run gen` must run FIRST. The corpus lives in `corpus-data.json` now, which
     gen writes and vite copies. Skip it and you ship stale data or none.
  2. **The bundle used to fetch `/corpus-data.json`, root-absolute.** That only
     resolves when the app is served from a domain root; under
     `/wp-content/themes/…/report-graph/` it 404s, so a straight rebuild-and-drop
     would have shipped a *broken* graph. Fixed in Reports Clustering at
     `src/data/browserCorpus.ts` — it now fetches `import.meta.env.BASE_URL +
     'corpus-data.json'`, which is correct in dev and under any `--base`. **That is
     a change in Thomas's other repo**, one line plus a comment.
  3. **The `noindex` meta is not in the graph repo's `index.html`.** It has always
     been hand-added to the deployed copy, so every rebuild silently drops it. It
     was re-added. The `X-Robots-Tag` header in `functions.php` is the only backstop.
- **Reports Clustering's `node_modules` was installed on Windows**, so rollup and
  esbuild had only win32 binaries and the build died with `MODULE_NOT_FOUND` under
  the Linux mount. `npm i --no-save @rollup/rollup-linux-x64-gnu` pulled in every
  platform's binaries; **the win32 ones are still there**, so his Windows build is
  unaffected.
- **Committing a file to the device re-encodes images.** Verify a committed image by
  staging it back, not by trusting the byte count.
- **The Facebook rule.** His public Facebook is linked from thomascheesman.ca. He has
  ruled it "between you and me only" — never material for this site.

## 3. Current design

Light paper, near-black ink, one deep-teal accent (`#0F5F6B`), dark mode via
`prefers-color-scheme`. Familjen Grotesk / Source Serif 4 / IBM Plex Mono. Confirmed;
stop re-litigating it.

**Standing rules:**
- `object-fit: contain`, never `cover`.
- Content must render without JavaScript. JS allowed; dependencies and a build step
  are not — prebuilt bundles copied into `assets/` only.
- Fonts self-hosted. No third-party font request.
- No phone number on the site. It is in the résumé PDF.
- **Never publish an exact node, edge, report or grade count** *in copy*. Round or
  describe. Thomas ruled one exception on 2026-09-16: the application screenshot on
  Projects shows the app's own counters, and he accepted that. The prose still
  rounds, and the image will date — swap it when the numbers drift far.
- **No vanity metrics.** No line counts.
- **Do not invent dates or figures.** Unknown → ask Thomas.
- Hajdu-Cheney syndrome is named on purpose. Symptom detail is not.
- WordPress stays once per spec table as a hiring keyword; out of headline prose.
- Nothing familial, nothing from Facebook.
- The GPRS logo is not this site's favicon.
- Edit `resume/resume-source.html` and re-render. Never edit the PDF. Measure page
  count after any content change; the target is exactly two pages, and page 2 is full.
- Melanie and Thomas are "the parents" of the three children, never "co-parents".

**Demo rules:**
- **The 3D canvas keeps its own dark ground in both themes** — the same `#060913`
  the stills sit on. The panel under it stays on paper, because it is text.
- **Nothing heavy loads before a click.** The renderer is 1.3MB.
- **`basis` is quoted, never paraphrased.** The exporter may drop retrieval chatter
  and truncate; it may not rewrite a claim or soften a grade.
- **The written chain under the demo is the accessible equivalent**, not decoration.
- **Nothing rearranges the layout on interaction.** Dim, highlight and fly the
  camera; never move a node to make a reading easier than the data earned.

## 4. Open items — carry these forward until closed

### COPY
| # | Item | Notes |
|---|---|---|
| C-2 | Demo and Projects copy unread by Thomas | The demo block, the new figure caption, and the four-sites sentences are all mine. 404 copy he has ruled acceptable for now. |
| C-5 | **`page-hcs.php` arithmetic now inconsistent** | That paragraph says two years up the line then "kitchen manager for 10 years". From 2002 that lands in 2014, not 2013. Changing the Keg start exposed it. Thomas ruled the start and the total; he has not ruled this number, and it is his own voice. Ask, do not guess. |

### GRAPH
| # | Item | Notes |
|---|---|---|
| G-4 | **Rocket Lander — page built, build not yet exported** | `lander.html` + `lander.js` written and tested against a stub. Drop the Godot export into `public/lander/` (its own `index.html` at that folder's root) and the page works. **Export with Thread Support OFF** — that avoids the COOP/COEP headers entirely, which is why no `_headers` file was added. |
| G-9 | **Backtick must replace Tab in `tuning.gd`** | The page documents <kbd>`</kbd> for the tuning panel. Until the build agrees, the page is wrong. Tab cannot be used in a browser: it is the keyboard user's only exit from the canvas. |
| G-10 | No still for the lander frame | The frame is bare dark ground with the button centred on it. A screenshot of the build mid-flight would fill it; same treatment as the graph poster. |
| G-11 | **`V0.2.md` in the lander repo says "NEVER RUN"** | It is dated 2026-07-26 and titled "the BUILT BUT UNFLOWN session". Thomas has since flown it — it flies, the landing is unsolved. The repo's top doc therefore contradicts both the site and reality, and `CLAUDE.md`'s "currently a physics drop test" and `rocket.md`'s roadmap are stale with it. **Fix before the repo goes public**, or the first thing a reader sees is a contradiction. A V0.3 saying "runs, flies, landing unsolved, tuning is the work" closes it. |
| G-12 | Repo not yet public | Thomas asked for public. Content scanned and clean — no email, no family names, no health detail, no address; only `C:\Users\thoma\...` paths in the docs, and `.gitignore` already excludes `.godot/`. **Blocked on G-11 only.** No `gh` CLI on the machine: the route is GitHub Desktop → Add Local Repository → Publish repository, with "Keep this code private" unticked. |
| G-6 | Demo still is a headless render | The application screenshot replaced the *opening figure*, not the demo still — the still must show what the button loads. If Thomas wants a hand-framed still of the gp-budget slice, he can screenshot the live demo once it is deployed and it swaps in. |
| G-8 | No keyboard path into the 3D scene | Thomas: "the site and projects need solid accessibility passes." Its own session — see A11Y below. |

### A11Y
| # | Item | Notes |
|---|---|---|
| A-1 | **Full accessibility pass, site-wide** | Thomas asked for it on the site and on Projects specifically. Not started. Should be its own session, not bolted onto feature work: keyboard order, focus states, landmarks, contrast in both themes, the canvas's text equivalent, and the résumé PDF's tagging. |

### INFRA
| # | Item | Notes |
|---|---|---|
| INFRA-1 | workers.dev duplicate | **Config done; closes on the next deploy.** Confirm the workers.dev URL stops serving, then close. |
| INFRA-2 | Favicon is a placeholder | Thomas is finding the TC mark. |
| INFRA-4 | Permanent email undecided | `thomas@tc-ventures.ca` works; he wants a non-general address within weeks. |
| INFRA-5 | `_to_delete/` folders for Thomas | tc-ventures site: `gd-block.html`, `graph-demo.css`, `graph-everything.webp`. Reports Clustering: `dist-wp/` (the stale first build). |

### DOMAIN
| # | Item | Notes |
|---|---|---|
| D-1 | WordPress.com still claims the domain | Harmless; detach when convenient. |
| D-2 | WP.com plan auto-renew | Do not cancel anything without confirming DNS for the live sites is unaffected. |
| D-3 | Professional Email expiry ~2026-10-08 | Thomas may renew out of convenience. |

### OTHER REPOS
| # | Item | Notes |
|---|---|---|
| O-1 | **Graph bundle rebuilt — written, not uploaded** | In `tc-ventures-child-theme/assets/report-graph/`. Two things to know: the old `assets/index-ev_2RCV6.js` (2.4MB) is still there unreferenced because Cowork cannot delete it, and `corpus-data.json` is **~11 MB**, so `/reports-graph` now pulls roughly 13 MB versus 2.4 MB before. Confirm the host gzips `.json`; the page is unlisted, but that is still a heavy visit. |
| O-2 | `/projects` on thomascheesman.ca has third-person leakage | **Thomas is writing this himself.** Not an agent task. |
| O-3 | Keg dates | **Done** in three files. See C-5 for what it exposed. |
| O-4 | bareyourrare history still contains `permits/` and `3.jpg` | Instructions written to `_Quarantine\bareyourrare-history-purge.md`. Thomas runs it. Until then, treat the address as disclosed. |
| O-5 | `bareyr\.git` lock-file junk | Cosmetic; Thomas deletes. |

**Closed this session:** INFRA-1 (in config); G-1, G-2, G-5 (closed in 005); G-7
(built as hover-and-focus, not a magnifier, with his agreement); O-3; the
four-sites copy fix. **Nothing was killed.**

## 5. HOW TO WRITE THE NEXT HANDOFF

**This section is fixed. Copy it forward verbatim into every handoff.**

### Naming and location

- Handoffs live at repo root: `handoff-001.md`, `handoff-002.md`, `handoff-003.md`…
- **Zero-padded to three digits.** Sequential, never reused, never renumbered.
- One handoff per meaningful session. A session that changed nothing does not earn one.
- **The highest-numbered file is the only current one.** Everything below it is
  history. Do not read old handoffs unless you are chasing why a decision was made.

### Forking

Work will fork — design, contact page, the graph embed, the Godot build, copy.
**Do not fork the file series.** Numbering stays one straight line. Instead:

- Every open item carries a **workstream tag** in SCREAMING-CASE: `DESIGN`,
  `CONTACT`, `GRAPH`, `LANDER`, `COPY`, `INFRA`, `DOMAIN`.
- §4 (open items) is grouped by tag once there is more than one tag in play.
- If two agents work in parallel, each writes its own numbered handoff and the
  later one merges the earlier one's open items into its §4. Nothing is dropped
  silently — an item leaves §4 only when it is **done** or **explicitly killed by
  Thomas**, and the handoff says which.

### Required sections

Every handoff has these, in this order, with these numbers:

```
0. Read this first if you are a fresh agent
1. What this is            — stack table. Update only when the stack changes.
2. What was done           — THIS SESSION only. Not a changelog of all time.
3. Current design          — the rules a new agent would otherwise break.
4. Open items              — the table. Tagged. Carried forward.
5. HOW TO WRITE THE NEXT HANDOFF   — verbatim copy of this section.
6. Next up                 — what the next agent should actually do, in order.
```

### Rules

- **State, not narrative.** "The X is broken because Y" beats a story about
  discovering it. Nobody needs your journey.
- **Traps are worth more than successes.** If something cost you an hour, write it
  down in one sentence so it costs the next agent nothing. The stale-clone and
  `index.lock` notes in handoff-001 §2 are the model.
- **Never claim something is done that you have not verified.** "Deployed" means you
  saw it serve. "Written" means it is on disk. Say which.
- **Do not state git state and do not tell Thomas to commit.** That is his routine.
- Keep it under ~400 lines. If §2 is getting long, you are writing a diary.

## 6. Next up

In order:

1. **See it serve.** Deploy tc-ventures.ca and open `/projects.html` on the real
   domain: nothing heavy on load, the button appears, the scene renders, hover dims,
   an edge click fills the panel. Then confirm the workers.dev URL has stopped
   serving and close INFRA-1.
2. **Upload the rebuilt graph** to thomascheesman.ca and open `/reports-graph`. The
   one thing to check is that `corpus-data.json` loads from under the theme path —
   that is the bug this rebuild fixed, and a 404 there means the `--base` and the
   real location disagree.
3. **C-5**, the "10 years" line. One question to Thomas.
4. **Rocket Lander:** the backtick key change in `tuning.gd` (G-9), then export with
   Thread Support off, then drop the folder into `public/lander/`. Check on a phone —
   an iframe at 390px gets a 16:9 box with a 320px floor, and whether that is playable
   is a question only flying it answers. Then V0.3 (G-11) and publish (G-12).
5. **The accessibility pass** (A-1), as its own session.
6. **Favicon** when the mark arrives (INFRA-2).
